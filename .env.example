# Pixel Ride 环境变量示例
# 复制为 .env 后修改；.env 已被 gitignore，切勿提交真实密钥。

# Prisma generate 需要这个变量存在，但运行时不连接。
# 实际数据访问经 Cloudflare D1 binding（pixel_ride）。
DATABASE_URL="file:./dev.db"

# Session HMAC 密钥。本地 dev 用任意随机串即可。
# 生产环境经 `wrangler secret put SESSION_SECRET` 注入，不放在这里。
SESSION_SECRET="local-dev-secret-please-change-9f2a7c"
